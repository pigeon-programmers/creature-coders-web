const router = require('express').Router()
const Map = require('../db/models/Map')

module.exports = router

// router.get('/:id', async (req, res, next) => {
//   try {
//     const levelsGames = await Map.findByFk()
//   }
// })
